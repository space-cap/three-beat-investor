# Gemini Code Assistant Context

## Project Overview

This is a Python project named "three-beat-investor". Based on the name, it is likely intended to be a financial application, possibly related to stock market analysis or investment strategies. The project is in a very early stage of development.

The project structure includes a `src` directory for source code, a `tests` directory for tests, and a `docs` directory for documentation. It uses `pyproject.toml` for project metadata and dependency management, which is standard for modern Python projects.

## Building and Running

**Dependencies:**

*   Python 3.13+

**Running the application:**

```bash
python main.py
```

**Testing the application:**

```bash
# TODO: Add test commands once a test framework is in place.
```

## Development Conventions

*   **Code Style:** This project uses the `black` code formatter for consistent code style.
*   **Testing:** TODO: Define the testing framework and conventions.
*   **Commits:** TODO: Define commit message conventions.
